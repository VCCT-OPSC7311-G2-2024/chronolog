<?xml version="1.0" encoding="utf-8"?>
<!-- The root FrameLayout for the NotificationPage fragment -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".NotificationPage"
    android:background="@drawable/notificationpage_gradient">

    <!-- Button for clearing all notifications -->
    <Button
        android:id="@+id/clearAllBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="120dp"
        android:layout_marginStart="40dp"
        android:background="@drawable/rounded_button"
        android:textColor="@color/white"
        android:text="Clear All" />

    <!-- LinearLayout for the 'Notifications' text -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal"
        android:layout_marginTop="30dp">

        <!-- TextView for the 'Notifications' text -->
        <TextView
            android:id="@+id/textView8"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="90dp"
            android:layout_weight="1"
            android:textColor="@color/white"
            android:textSize="40sp"
            android:textStyle="bold"
            android:text="Notifications" />
    </LinearLayout>

    <!-- RelativeLayout for the RecyclerView and the line separator -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- RecyclerView for displaying the notifications -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="40dp"
            android:layout_marginTop="150dp"
            android:layout_marginEnd="4dp"
            android:layout_marginBottom="4dp"
            tools:itemCount="17"
            tools:listitem="@layout/notification_list" />

        <!-- View for the line separator -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginStart="40dp"
            android:layout_marginEnd="40dp"
            android:layout_below="@id/recyclerView"
            android:background="@android:color/white"/>

    </RelativeLayout>
</FrameLayout>